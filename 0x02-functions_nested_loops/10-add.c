#include "main.h"
/**
 * add - adds two integers ans return the result
 * @num1: the first integer
 * @num2: the second integer
 * Return: returns the sum of the two integers
 */
int add(int num1, int num2)
{
	return (num1 + num2);
}
