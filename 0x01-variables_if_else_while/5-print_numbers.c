#include <stdio.h>

/**
 * main - entry point
 * description - prints all single digit numbers of base 10
 * Return: always 0
 */
int main()
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d\n", i);
	return (0);
}
